AGFI 3-Day Program Image Gallery

Quick Vite + React + TypeScript single-page app showing OneDrive images grouped by day.

Run locally:

```bash
npm install
npm run dev
```

Edit social links in `src/constants/social.ts` and images in `src/data/images.ts`.
